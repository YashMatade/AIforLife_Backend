{
    "version": 2,
    "name": "native-server",
    "builds": [
        {
            "src": "server.js",
            "maxDuration": 10,
            "use": "@vercel/node"
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "/server.js"
        }
    ]
}