{
    "version": 2,
    "name": "native-server",
    "functions": {
        "api": {
            "src": "server.js",
            "maxDuration": 10
        }
    },
    "routes": [
        {
            "src": "/(.*)",
            "dest": "/server.js"
        }
    ]
}